import { StringToExpressionPipe } from './string-to-expression.pipe';

describe('StringToExpressionPipe', () => {
  it('create an instance', () => {
    const pipe = new StringToExpressionPipe();
    expect(pipe).toBeTruthy();
  });
});
